# --- EJERCICIO 15 ---
# Lambda que sume 3 a cada número de una lista dada (se suele usar con map).
lista_numeros = [1, 2, 3, 4]
sumar_tres = list(map(lambda x: x + 3, lista_numeros))

# print(sumar_tres)
